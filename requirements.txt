# Lightweight Vercel serverless dependencies. The dlib/OpenCV recognition
# stack is intentionally in requirements.local.txt because it runs locally
# with the private enrolled photos rather than in the cloud function.
Flask==3.0.3
Werkzeug==3.0.3
Jinja2==3.1.6
MarkupSafe==3.0.3
click==8.5.0
blinker==1.9.0
itsdangerous==2.2.0
