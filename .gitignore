# Profile data remains local. Reference images are deliberately versioned
# because the repository owner explicitly requested they be uploaded.
details.json

# Local Python runtime and generated files
.venv/
.python311/
__pycache__/
*.py[cod]
*.log
python-*.exe

# Android local configuration and build output
android_app/.gradle/
android_app/local.properties
android_app/app/build/

# Editor files
.vscode/
.idea/
